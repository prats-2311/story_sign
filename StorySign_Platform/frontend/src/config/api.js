/**
 * Simplified API Configuration for StorySign Platform
 * Direct backend connection without conditional logic
 */

// Direct backend URL for automatic connection
const API_BASE_URL = "http://127.0.0.1:8000";

// Export the base URL directly
export default API_BASE_URL;
